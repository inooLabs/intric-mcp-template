# JWT Authentication
# Used when auth=verifier is enabled on the FastMCP server

# Shared secret for HMAC verification (minimum 32 characters)
MCP_SERVER_JWT_SECRET=this-is-a-dummy-secret-for-development-only

# JWT issuer - must match the "iss" claim in incoming JWTs (optional)
MCP_SERVER_JWT_ISSUER=

# JWT audience - must match the "aud" claim in incoming JWTs (optional)
MCP_SERVER_JWT_AUDIENCE=
